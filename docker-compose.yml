version: "3.9"

services:
  postgres:
    image: postgres:17
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@local.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
      PGADMIN_CONFIG_ALLOW_SAVE_PASSWORD: 'True'
    ports:
      - "5050:80"
    volumes:
      - ./pgadmin/servers.json:/pgadmin4/servers.json
      - ./pgadmin/.pgpass:/tmp/.pgpass:ro
    entrypoint: >
      /bin/sh -c "
        cp /tmp/.pgpass /var/lib/pgadmin/.pgpass &&
        chown pgadmin:root /var/lib/pgadmin/.pgpass &&
        chmod 600 /var/lib/pgadmin/.pgpass &&
        /entrypoint.sh
      "

  nodeapp:
    image: node:20
    container_name: backend
    working_dir: /usr/src/app
    volumes:
      - "C:/Users/cathe/OneDrive/Área de Trabalho/kanbanize/kanbanize_back_end:/usr/src/app"
    command: sh -c "npm install && node app.js"
    ports:
      - "3001:3001"
    depends_on:
      - postgres
    environment:
      - DB_USER=postgres
      - DB_PASSWORD=admin
      - DB_DATABASE=postgres
      - DB_PORT=5432
      - PGHOST=postgres


  frontend:
    image: node:20
    container_name: frontend
    working_dir: /usr/src/app
    volumes:
      - ./kanbanize_front_end:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npm install && npm run dev -- --port 3000 --hostname 0.0.0.0"

    ports:
      - "3000:3000"
    environment:
      CHOKIDAR_USEPOLLING: "true"
      CHOKIDAR_INTERVAL: 100
      HOST: 0.0.0.0
      PORT: 3000


volumes:
  postgres_data:
